% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{sf_missing_columns}
\alias{sf_missing_columns}
\title{Columns the instrument declares that the responses left out}
\usage{
sf_missing_columns(x, ...)
}
\arguments{
\item{x}{An \code{sframe_quality_report} object.}

\item{...}{Passed to methods.}
}
\value{
A character vector of column names, empty where the export carried
every declared column.
}
\description{
A partial export gives a quality report every column it holds, and none of
the ones it dropped. This names the declared columns that never arrived, so
a missingness figure can be read against what was expected. They count as
missing for every respondent in \code{\link[=quality_report]{quality_report()}}'s rates.
}
\examples{
demo <- sframe_demo_data()
qr   <- quality_report(demo$responses, demo$instrument)
sf_missing_columns(qr)
}
\seealso{
\code{\link[=quality_report]{quality_report()}}, \code{\link[=sf_flagged]{sf_flagged()}}
}
