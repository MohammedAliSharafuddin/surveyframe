% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_write_sframe.R
\name{write_sframe}
\alias{write_sframe}
\title{Write an instrument to a .sframe file}
\usage{
write_sframe(
  instrument,
  path,
  pretty = TRUE,
  overwrite = FALSE,
  new_instrument = FALSE
)
}
\arguments{
\item{instrument}{An \code{sframe} object created by \code{\link[=sf_instrument]{sf_instrument()}}.}

\item{path}{Character. The file path to write to. The \code{.sframe} extension
is appended automatically if not already present.}

\item{pretty}{Logical. Whether to write formatted JSON with indentation.
Defaults to \code{TRUE}. Set to \code{FALSE} for compact files.}

\item{overwrite}{Logical. Whether to overwrite an existing file. Defaults
to \code{FALSE}.}

\item{new_instrument}{Logical. \code{TRUE} declares that the content is a new
instrument, not a revision of the one it was read from, so no amendment is
required. The instrument must carry no amendment log. Defaults to \code{FALSE}.}
}
\value{
The file path, invisibly.
}
\description{
Serialises an \code{sframe} instrument object to a UTF-8 JSON file with a
SHA-256 integrity hash. The instrument is always validated before writing,
and an invalid instrument is refused. The hash is computed over a canonical
serialisation of the content with \code{hash.value} set to an empty string: object
keys are sorted, so it identifies content, not the exact bytes.
}
\section{Revisions and the amendment log}{
Writing checks the amendment log recorded by \code{\link[=amend_sframe]{amend_sframe()}}. Each entry must
follow the one before it, the instrument must still match its last recorded
amendment, and an instrument read with \code{\link[=read_sframe]{read_sframe()}} must keep every
amendment it was read with. Content changed since it was read is refused
unless the change was recorded with \code{\link[=amend_sframe]{amend_sframe()}}. To publish changed
content as a different instrument, remove its amendment log and set
\code{new_instrument = TRUE}.

These are checks on the content in hand. The hash is unsigned and can be
recomputed by anyone who edits a file, so it does not establish who wrote an
instrument or when.
}

\examples{
instr <- read_sframe(
  system.file("extdata", "tourism_services_demo.sframe",
              package = "surveyframe")
)
out <- write_sframe(instr, tempfile(fileext = ".sframe"))
file.exists(out)
}
\seealso{
\code{\link[=read_sframe]{read_sframe()}}, \code{\link[=validate_sframe]{validate_sframe()}}
}
