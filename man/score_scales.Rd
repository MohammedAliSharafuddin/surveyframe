% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score_scales.R
\name{score_scales}
\alias{score_scales}
\title{Score defined scales from survey responses}
\usage{
score_scales(data, instrument, keep_items = TRUE, keep_meta = TRUE)
}
\arguments{
\item{data}{A \code{tibble} or \code{data.frame} of responses.}

\item{instrument}{An \code{sframe} object.}

\item{keep_items}{Logical. Whether to retain individual item columns in the
output. Defaults to \code{TRUE}.}

\item{keep_meta}{Logical. Whether to retain non-item columns (metadata) in
the output. Defaults to \code{TRUE}.}
}
\value{
A \code{tibble} with scored scale columns appended. Scale columns are
named using the scale \code{id}.
}
\description{
Applies scale scoring rules from the instrument to response data. Handles
reverse coding, composite score computation (mean or sum), and minimum
valid item thresholds. Returns a data frame with one scored column per
scale.
}
\examples{
\dontrun{
scored <- score_scales(responses, instr)
}
}
\seealso{
\code{\link[=sf_scale]{sf_scale()}}, \code{\link[=reliability_report]{reliability_report()}}
}
