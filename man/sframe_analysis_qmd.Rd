% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demo_library.R
\name{sframe_analysis_qmd}
\alias{sframe_analysis_qmd}
\title{Write a Quarto analysis notebook for any instrument}
\usage{
sframe_analysis_qmd(
  instrument,
  data,
  dir = ".",
  basename = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{instrument}{An \code{sframe} object.}

\item{data}{A \code{data.frame} of responses, read by \code{\link[=read_responses]{read_responses()}} when
the notebook runs.}

\item{dir}{Directory to write into. Defaults to the working directory.}

\item{basename}{Character or \code{NULL}. File base name shared by the \code{.qmd},
\code{.sframe}, and \verb{_responses.csv} files. Defaults to a slug of the
instrument's title.}

\item{overwrite}{Logical. Overwrite existing files of the same name.}
}
\value{
A list with \code{qmd}, \code{sframe}, and \code{csv} paths, invisibly.
}
\description{
Unlike \code{\link[=sframe_demo_qmd]{sframe_demo_qmd()}}, which only works for one of the bundled demo
instruments (it looks \code{name} up in \code{\link[=sframe_demos]{sframe_demos()}}), this writes a
runnable Quarto notebook for any instrument, using its own responses. The
notebook reads the instrument and its responses back from 2 companion
files written alongside it, so all 3 files must stay together.
}
\examples{
item  <- sf_item("q1", "How satisfied are you?", type = "text")
instr <- sf_instrument("Demo", components = list(item))
resp  <- data.frame(q1 = c("Great", "Fine"))
out <- sframe_analysis_qmd(instr, resp, dir = tempdir())
basename(out$qmd)
}
\seealso{
\code{\link[=sframe_demo_qmd]{sframe_demo_qmd()}}, \code{\link[=write_sframe]{write_sframe()}}, \code{\link[=read_responses]{read_responses()}}
}
