% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psychometrics.R
\name{reliability_report}
\alias{reliability_report}
\title{Compute reliability statistics for scored scales}
\usage{
reliability_report(data, instrument, scales = NULL, alpha = TRUE, omega = TRUE)
}
\arguments{
\item{data}{A \code{tibble} or \code{data.frame} of responses. Item columns must be
present.}

\item{instrument}{An \code{sframe} object.}

\item{scales}{Character vector or NULL. A subset of scale IDs to analyse.
When NULL (default), all scales in the instrument are included.}

\item{alpha}{Logical. Whether to compute Cronbach's alpha. Defaults to
\code{TRUE}.}

\item{omega}{Logical. Whether to compute McDonald's omega. Defaults to
\code{TRUE}.}
}
\value{
An object of class \code{sframe_reliability_report}, a list with one
element per scale. Each element is a list of statistics and a summary
tibble.
}
\description{
Produces Cronbach's alpha and McDonald's omega for each scale defined in
the instrument, along with the number of items and sample size.
}
\examples{
\dontrun{
rr <- reliability_report(responses, instr)
print(rr)
}
}
\seealso{
\code{\link[=sf_scale]{sf_scale()}}, \code{\link[=item_report]{item_report()}}
}
