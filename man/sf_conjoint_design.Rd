% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conjoint_design.R
\name{sf_conjoint_design}
\alias{sf_conjoint_design}
\title{Declare a choice-experiment (conjoint) design}
\usage{
sf_conjoint_design(
  id,
  attributes,
  method = c("full", "balanced", "random"),
  n_profiles = NULL,
  n_alternatives = 2L,
  n_tasks = NULL,
  blocks = 1L,
  seed = NULL,
  profiles = NULL,
  label = NULL
)
}
\arguments{
\item{id}{Design identifier. Must start with a letter and contain only
letters, numbers, and \verb{_} characters.}

\item{attributes}{Named list of character vectors, one per attribute,
giving that attribute's levels. At least 2 attributes, each with at
least 2 levels.}

\item{method}{One of \code{"full"}, \code{"balanced"}, or \code{"random"}. Ignored when
\code{profiles} is supplied.}

\item{n_profiles}{Number of profiles to keep. Required for \code{"balanced"}
and \code{"random"}, ignored for \code{"full"}.}

\item{n_alternatives}{Alternatives shown per choice task, default 2.}

\item{n_tasks}{Choice tasks per block. Defaults to as many whole tasks as
the profile set supports.}

\item{blocks}{Number of blocks the tasks are split across, default 1.}

\item{seed}{Integer seed. Generated and stored when not supplied.}

\item{profiles}{Optional data frame of pre-built profiles, one column per
attribute. Supplying this bypasses generation and declares the design as
given.}

\item{label}{Human-readable label.}
}
\value{
An object of class \code{sf_conjoint_design} with \verb{$profiles}, \verb{$tasks}
(long format, one row per block, task, and alternative, ready for a
choice model), \verb{$balance}, and the declaration that produced them.
}
\description{
Generates and stores a conjoint profile set and task schedule as part of
the instrument's pre-declared contract. This is a design generator, not an
estimator: it fixes what respondents will be shown, and it does not fit or
analyse choice models.
}
\details{
The design is reproducible by construction. \code{seed} is always recorded, and
generated when not supplied, so regenerating from the stored declaration
returns the identical profiles and tasks.
}
\section{Choosing a method}{

\code{"full"} enumerates every combination, which is exact but grows fast: 4
attributes at 3 levels each is 81 profiles. \code{"random"} takes a seeded
random subset of that size. \code{"balanced"} samples repeatedly and keeps the
subset with the most even level spread and the weakest association between
attributes.

\code{"balanced"} is a search, not a construction. It does not produce a
catalogued orthogonal fractional factorial and does not claim the
guarantees of one. The achieved balance is reported in \verb{$balance} so the
design can be inspected rather than trusted. A study needing a specific
D-optimal or orthogonal design should generate it elsewhere and pass it in
through \code{profiles}, which keeps the declaration in the contract either way.
}

\examples{
design <- sf_conjoint_design(
  "hotel_dce",
  attributes = list(
    price    = c("50", "100", "150"),
    board    = c("room only", "breakfast"),
    distance = c("beachfront", "10 min walk")
  ),
  method = "balanced", n_profiles = 6, n_alternatives = 2, seed = 42
)
design$profiles
design$tasks
}
\seealso{
\code{\link[=sf_instrument]{sf_instrument()}}
}
