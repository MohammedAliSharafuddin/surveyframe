% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{as_sframe}
\alias{as_sframe}
\alias{as_sframe.sframe}
\alias{as_sframe.sframe_validation}
\title{Coerce to an instrument}
\usage{
as_sframe(x, ...)

\method{as_sframe}{sframe}(x, ...)

\method{as_sframe}{sframe_validation}(x, ...)
}
\arguments{
\item{x}{An \link{sframe_validation} object or an \code{sframe}.}

\item{...}{Passed to methods.}
}
\value{
An \code{sframe} object. When the validation passed, its
\code{meta$validated} is \code{TRUE}.
}
\description{
Recovers the \code{sframe} instrument from a validation diagnostic. This is the
migration path for code that used the \code{strict = TRUE} return of
\code{\link[=validate_sframe]{validate_sframe()}} as an instrument, which it no longer is.
}
\examples{
item  <- sf_item("q1", "How satisfied are you?", type = "text")
instr <- sf_instrument("Demo", components = list(item))

validated <- as_sframe(validate_sframe(instr, strict = TRUE))
isTRUE(sf_meta(validated)$validated)
}
\seealso{
\code{\link[=validate_sframe]{validate_sframe()}}, \link{sframe_validation}
}
