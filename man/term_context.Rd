% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text_analysis.R
\name{term_context}
\alias{term_context}
\title{Keyword-in-context concordance for open-ended text}
\usage{
term_context(text, term, window = 6L, max_matches = 20L)
}
\arguments{
\item{text}{Character vector of responses, ideally already cleaned by
\code{\link[=clean_text_responses]{clean_text_responses()}}.}

\item{term}{Character. A single keyword to search for.}

\item{window}{Integer. Maximum number of words of context to keep before
and after each match. Default \code{6}.}

\item{max_matches}{Integer. Maximum number of matches to return, counted
across all responses. Default \code{20}.}
}
\value{
A data.frame with columns \code{respondent}, \code{before}, \code{match}, and
\code{after}.
}
\description{
Finds every case-insensitive, whole-word match of \code{term} in \code{text} and
returns the words immediately before and after each match, so a reader
can judge how a term is actually being used rather than reading a bare
frequency count. Matching is on whole words only, so searching for
"room" does not match "roomy". \code{text} is expected to already have been
run through \code{\link[=clean_text_responses]{clean_text_responses()}} (its \code{respondent} attribute is used
to cite the original row index for each match; when absent, positions
\code{seq_along(text)} are used instead). Tokenisation here is a plain
whitespace split, not the internal stop-word-stripping tokeniser behind
\code{\link[=term_frequency]{term_frequency()}}: stop words are part of a match's context and
stripping them would corrupt the very thing a concordance is for.
}
