% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demo_library.R
\name{sframe_demo}
\alias{sframe_demo}
\title{Load one bundled demo}
\usage{
sframe_demo(name, branded = FALSE)
}
\arguments{
\item{name}{Character. A demo name, as listed by \code{\link[=sframe_demos]{sframe_demos()}}.}

\item{branded}{Logical. When \code{TRUE}, the instrument comes back with the
standard welcome page, logo, theme colour and thank you page spliced into
its \code{render} block. The bundled file on disk is left unchanged, so the
same branding can be shown on whichever demo matches your own survey. See
\code{\link[=sframe_demo_branding]{sframe_demo_branding()}}.}
}
\value{
A list with \code{instrument}, \code{responses}, and the paths behind them:
\code{instrument_path}, \code{responses_path}, \code{codebook_path} and \code{results_path}.
The codebook carries variable and value labels, so the data means
something outside R. The results table is what surveyframe reports for
this demo, which is the reference to compare against when you run the
same data through another package.
}
\description{
Load one bundled demo
}
\examples{
demo <- sframe_demo("two_group")
demo$instrument
head(demo$responses)
}
\seealso{
\code{\link[=sframe_demos]{sframe_demos()}}, \code{\link[=sframe_export_labelled]{sframe_export_labelled()}}
}
