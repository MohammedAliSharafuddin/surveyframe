% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text_analysis.R
\name{ngram_frequency}
\alias{ngram_frequency}
\title{N-gram frequency for open-ended text}
\usage{
ngram_frequency(text, n = 2L, stop_words = NULL, top_n = 30L)
}
\arguments{
\item{text}{Character vector of responses (raw or already cleaned by
\code{\link[=clean_text_responses]{clean_text_responses()}}).}

\item{n}{Integer. N-gram size. Default \code{2} (bigrams).}

\item{stop_words}{Character vector of words to exclude, or \code{NULL} to use
the built-in English list, or \code{character(0)} for no filtering.}

\item{top_n}{Integer. Maximum number of n-grams to return, most frequent
first. Default \code{30}.}
}
\value{
A data.frame with columns \code{term} (the space-joined n-gram), \code{n},
and \code{pct}.
}
\description{
Tokenises \code{text} via the same tokeniser as \code{\link[=term_frequency]{term_frequency()}} (whitespace
splitting, lower-casing, punctuation stripping, and stop-word removal),
then slides a window of \code{n} tokens across each response's token vector
and counts how often each resulting n-gram occurs. \code{n = 2} (the default)
gives bigrams; \code{n = 3} gives trigrams. Because stop words are already
removed by the shared tokeniser, an n-gram never straddles a dropped
word; it is built only from tokens that survive filtering, in their
original order within each response.
}
