% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{sframe_plot_decision_ranking}
\alias{sframe_plot_decision_ranking}
\title{Ranked-score bar chart for a decision-family result}
\usage{
sframe_plot_decision_ranking(result, palette = c("web", "print"))
}
\arguments{
\item{result}{A decision-family result list from \code{\link[=run_analysis_plan]{run_analysis_plan()}},
carrying either \code{scores} and \code{alternatives} or a ranking \code{table}.}

\item{palette}{One of \code{"web"} or \code{"print"}. See \code{sframe_brand()}.}
}
\value{
A ggplot2 object, or \code{NULL} when the result carries no ranking.
}
\description{
The shared chart for every MCDM ranking method: one horizontal bar per
alternative, ordered best first, with the leading alternative picked out.
It is generic over the method rather than tied to one, so AHP criterion
weights and any ranking method's scores all draw through it. The score
column is whatever the method reports as its headline quantity (a
closeness coefficient, a net flow, a priority weight), so the axis is
labelled from the result rather than hard-coded.
}
\seealso{
\code{\link[=run_analysis_plan]{run_analysis_plan()}}
}
