% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decision_data.R
\name{sframe_aggregate_judgements}
\alias{sframe_aggregate_judgements}
\title{Aggregate individual judgement matrices}
\usage{
sframe_aggregate_judgements(
  matrices,
  method = c("geometric", "arithmetic"),
  cr_filter = FALSE
)
}
\arguments{
\item{matrices}{Either a list of square numeric matrices or the object
returned by \code{\link[=sframe_assemble_pairwise]{sframe_assemble_pairwise()}}.}

\item{method}{\code{"geometric"} (the AHP default) or \code{"arithmetic"} (DEMATEL).}

\item{cr_filter}{Logical. When \code{TRUE}, individual matrices with a
consistency ratio at or above 0.10 are dropped before aggregation.
Applies to reciprocal matrices only. Default \code{FALSE}.}
}
\value{
A list with \code{matrix}, \code{method}, \code{n_respondents}, \code{n_dropped},
\code{n_dropped_consistency}, and \code{consistency} (the per-respondent CR
distribution, or \code{NULL} for a non-reciprocal set).
}
\description{
Aggregation of individual judgements (AIJ) across respondents. The
element-wise geometric mean is the standard choice for reciprocal AHP
matrices, because it is the only mean that preserves reciprocity: the
arithmetic mean of \code{m[a, b]} and the arithmetic mean of \code{m[b, a]} are not
reciprocals of each other. DEMATEL matrices are not reciprocal, so they
aggregate arithmetically.
}
\examples{
m1 <- matrix(c(1, 3, 1 / 3, 1), 2, 2)
m2 <- matrix(c(1, 5, 1 / 5, 1), 2, 2)
sframe_aggregate_judgements(list(m1, m2))$matrix
}
\seealso{
\code{\link[=sframe_assemble_pairwise]{sframe_assemble_pairwise()}}
}
