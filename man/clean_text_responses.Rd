% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text_analysis.R
\name{clean_text_responses}
\alias{clean_text_responses}
\title{Clean open-ended text responses for analysis}
\usage{
clean_text_responses(
  data,
  item_id,
  lowercase = TRUE,
  remove_punct = TRUE,
  strip_numbers = FALSE,
  instrument = NULL
)
}
\arguments{
\item{data}{A data.frame of responses.}

\item{item_id}{Character. The text/textarea item's column name.}

\item{lowercase}{Logical. Lower-case the text. Default \code{TRUE}.}

\item{remove_punct}{Logical. Strip punctuation. Default \code{TRUE}.}

\item{strip_numbers}{Logical. Strip digits. Default \code{FALSE}.}

\item{instrument}{Optional \code{sframe} instrument. When supplied, \code{item_id}
is validated as a \code{"text"} or \code{"textarea"} item before cleaning.}
}
\value{
A character vector of cleaned responses, with an integer
\code{"respondent"} attribute giving each entry's original row index in
\code{data}, so quotes extracted later can cite a respondent.
}
\description{
Extracts one text/textarea item's responses from a response data frame and
applies light, configurable cleaning: lower-casing, punctuation removal,
and optional number stripping. Blank and missing responses are dropped
rather than kept as empty strings, since they carry no term-frequency
signal and would otherwise inflate downstream response counts.
}
