% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/amendments.R
\name{amendment_log}
\alias{amendment_log}
\title{Read an instrument's amendment log}
\usage{
amendment_log(instrument)
}
\arguments{
\item{instrument}{An \code{sframe} object.}
}
\value{
A data frame with columns \code{timestamp}, \code{reason_code},
\code{reason_text}, \code{tier}, \code{author}, \code{deviation_report}, \code{signoff},
\code{previous_hash}, \code{new_hash}, and \code{changed_fields} (a comma-joined
string). Zero rows if the instrument has no recorded amendments.
Export with \code{write.csv()} for an external audit trail.
}
\description{
Returns the disclosed-amendment history recorded by \code{\link[=amend_sframe]{amend_sframe()}} as a
data frame, one row per amendment in the order they were recorded.
}
\examples{
item  <- sf_item("q1", "How satisfied are you?", type = "text")
instr <- sf_instrument("Demo", components = list(item))
amendment_log(instr)
}
\seealso{
\code{\link[=amend_sframe]{amend_sframe()}}
}
