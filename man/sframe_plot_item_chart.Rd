% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{sframe_plot_item_chart}
\alias{sframe_plot_item_chart}
\title{Item distribution chart, ggplot2 equivalent of the dashboard/studio panel}
\usage{
sframe_plot_item_chart(
  item,
  col_data,
  choice_set = NULL,
  palette = c("web", "print")
)
}
\arguments{
\item{item}{A list with at least \code{type} and \code{label} (an sframe item).}

\item{col_data}{The response column for this item.}

\item{choice_set}{A list with \code{values} and \code{labels} (an sframe choice
set), or \code{NULL} if the item has none.}

\item{palette}{One of \code{"web"} or \code{"print"}. See \code{\link[=sframe_brand]{sframe_brand()}}.}
}
\value{
A ggplot2 object, or \code{NULL} if this item type/data is unsupported.
}
\description{
Shared by \code{launch_dashboard()} (\code{inst/shiny/dashboard/app.R}) and the
SurveyStudio dashboard tab (\code{inst/shiny/app.R}), which otherwise
duplicated this base-graphics chart. Callers fall back to their own base
graphics when this returns \code{NULL} (ggplot2 not installed, unsupported
item type, or no data), so the dashboard keeps working without ggplot2.
}
\keyword{internal}
