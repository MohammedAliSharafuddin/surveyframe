% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{sframe_draw_mosaic}
\alias{sframe_draw_mosaic}
\title{Mosaic plot for a two-way categorical result}
\usage{
sframe_draw_mosaic(result, palette = c("web", "print"))
}
\arguments{
\item{result}{A \code{crosstab}/\code{chi_square} result list with a contingency
\code{table}.}

\item{palette}{One of \code{"web"} or \code{"print"}. See \code{sframe_brand()}.}
}
\value{
Invisibly \code{NULL}, called for its plotting side effect on the
current graphics device.
}
\description{
Base-graphics mosaic plot (via \code{\link[graphics:mosaicplot]{graphics::mosaicplot()}}), matching the
existing base-graphics precedent in this file
(\code{\link[=sframe_draw_likert_diverging]{sframe_draw_likert_diverging()}}) so it renders without ggplot2. An
alternative view of the same crosstab data
\code{sframe_plot_crosstab()} renders as a grouped bar. Use whichever reads
better for the table's shape (mosaic scales better to unbalanced group
sizes).
}
\examples{
instr <- sf_instrument("Crosstab demo", components = list(
  sf_item("arm", "Arm", type = "text"),
  sf_item("outcome", "Outcome", type = "text")
))
sf_plan(instr) <- list(list(
  id = "RQ1", research_question = "Does the outcome differ by arm?",
  family = "categorical", method = "chi_square",
  roles = list(row = "arm", column = "outcome")
))

responses <- data.frame(
  arm     = rep(c("control", "treatment"), each = 20),
  outcome = c(rep(c("yes", "no"), c(6, 14)), rep(c("yes", "no"), c(15, 5)))
)
res <- run_analysis_plan(responses, instr)
sframe_draw_mosaic(res$RQ1)
}
\seealso{
\code{sframe_plot_crosstab()}
}
\keyword{internal}
