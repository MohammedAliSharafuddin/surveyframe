% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/launch_studio.R
\name{launch_studio}
\alias{launch_studio}
\title{Launch the SurveyStudio interface}
\usage{
launch_studio(
  instrument = NULL,
  responses = NULL,
  respondent_id = NULL,
  submitted_at = NULL,
  meta_cols = NULL,
  strict = TRUE,
  screen = "auto",
  port = NULL,
  host = "127.0.0.1",
  launch.browser = interactive()
)
}
\arguments{
\item{instrument}{An \code{sframe} object or NULL.}

\item{responses}{A data.frame, tibble, CSV file path, or NULL.}

\item{respondent_id}{Character or NULL. Response ID column when \code{responses}
is a CSV path.}

\item{submitted_at}{Character or NULL. Submission time column when
\code{responses} is a CSV path.}

\item{meta_cols}{Character vector or NULL. Metadata columns when \code{responses}
is a CSV path.}

\item{strict}{Logical. Passed to \code{\link[=read_responses]{read_responses()}} when \code{responses} is a
CSV path.}

\item{screen}{The screen to open on. One of \code{"auto"}, which picks by what
you supply, or a screen name: \code{"open"}, \code{"amendments"}, \code{"preview"},
\code{"responses"}, \code{"quality"}, \code{"reliability"}, \code{"analysis"},
\code{"dashboard"} or \code{"export"}. \code{"data"} is accepted for \code{"responses"}.}

\item{port}{TCP port for the Shiny server.}

\item{host}{Host address passed to \code{\link[shiny:runApp]{shiny::runApp()}}.}

\item{launch.browser}{Whether to open the browser automatically.}
}
\value{
Called for its side effect.
}
\description{
Opens the SurveyStudio Shiny application, the visual interface for working
with an instrument that already exists. Its screens open an instrument,
record and read amendments, preview the survey, upload responses, review
data quality, inspect reliability, work on the analysis plan, read the
dashboard, and export.
}
\details{
Studio reads and analyses an instrument. To author one, question by
question, use \code{\link[=launch_builder]{launch_builder()}}, and open the result here.
}
\examples{
\dontrun{
launch_studio()

demo <- sframe_demo_data()
launch_studio(instrument = demo$instrument, launch.browser = FALSE)

launch_studio(
  instrument    = demo$instrument,
  responses     = demo$responses,
  respondent_id = "respondent_id",
  submitted_at  = "submitted_at"
)
}
}
\seealso{
\code{\link[=launch_builder]{launch_builder()}}, \code{\link[=launch_dashboard]{launch_dashboard()}}, \code{\link[=read_sframe]{read_sframe()}},
\code{\link[=read_responses]{read_responses()}}
}
