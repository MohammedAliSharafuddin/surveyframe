% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{sframe_likert_scale_groups}
\alias{sframe_likert_scale_groups}
\title{Group a scale's Likert items for a combined diverging chart}
\usage{
sframe_likert_scale_groups(instrument)
}
\arguments{
\item{instrument}{An \code{sframe} object.}
}
\value{
A named list, one entry per eligible scale (named by scale id),
each a list with \code{scale_id}, \code{title} (the scale's label), \code{items} (the
member item objects, in scale order), and \code{choice_set} (the shared
choice set object). Empty list if no scale qualifies.
}
\description{
Identifies which of an instrument's scales are eligible for one grouped
diverging chart across their member items (\code{\link[=sframe_plot_likert_scale]{sframe_plot_likert_scale()}}),
the same way a \code{"matrix"} question's rows are grouped
(\code{\link[=sframe_plot_likert_matrix]{sframe_plot_likert_matrix()}}): every member item is \code{"likert"} type and
all share one choice set. Scales that mix response scales, that resolve
to fewer than 2 qualifying items, or whose choice set cannot be found are
left out and fall back to one chart per item in the report's Response
distributions section.
}
\seealso{
\code{\link[=sframe_plot_likert_scale]{sframe_plot_likert_scale()}}, \code{\link[=sf_scale]{sf_scale()}}
}
