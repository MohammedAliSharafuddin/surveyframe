% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demo_library.R
\name{sframe_export_labelled}
\alias{sframe_export_labelled}
\title{Write responses to SPSS or Stata with the labels attached}
\usage{
sframe_export_labelled(data, instrument, path)
}
\arguments{
\item{data}{A response data frame, as returned by \code{\link[=read_responses]{read_responses()}}.}

\item{instrument}{The \code{sframe} the responses were collected with.}

\item{path}{Output file. An \code{.sav} is written for SPSS, a \code{.dta} for Stata,
chosen from the extension.}
}
\value{
The path, invisibly.
}
\description{
A plain CSV carries codes. \code{dm_1} arrives in SPSS as a column of integers
with no variable label and no value labels, and the reader has to
reconstruct all of it from the questionnaire. This attaches both from the
instrument: the variable label is the item's \code{label}, and the value labels
come from the item's choice set, which stores its \code{values} and \code{labels}
side by side.
}
\examples{
\donttest{
demo <- sframe_demo("two_group")
out <- file.path(tempdir(), "two_group.sav")
if (requireNamespace("haven", quietly = TRUE)) {
  sframe_export_labelled(demo$responses, demo$instrument, out)
}
}
}
\seealso{
\code{\link[=sframe_demo]{sframe_demo()}}, \code{\link[=read_responses]{read_responses()}}
}
