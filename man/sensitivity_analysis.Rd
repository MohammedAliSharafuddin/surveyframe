% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decision_sensitivity.R
\name{sensitivity_analysis}
\alias{sensitivity_analysis}
\title{Test how far a decision ranking moves when the weights are perturbed}
\usage{
sensitivity_analysis(
  x,
  weights,
  criteria_types,
  method = "topsis",
  delta = 0.05,
  alternatives = NULL,
  criteria = NULL,
  ...
)
}
\arguments{
\item{x}{Numeric performance matrix, alternatives in rows and criteria in
columns.}

\item{weights}{Numeric weight vector, one per criterion. Renormalised to
sum to 1 before use.}

\item{criteria_types}{Character vector of \code{"benefit"} or \code{"cost"}, one per
criterion.}

\item{method}{Ranking method. One of \code{"topsis"}, \code{"vikor"}, \code{"moora"},
\code{"smart"}, \code{"waspas"}, \code{"promethee"}, or \code{"electre"}.}

\item{delta}{Perturbation size as a proportion of the weight, default
\code{0.05}. A weight of 0.40 with \code{delta = 0.05} is tested at 0.42 and 0.38
before renormalisation.}

\item{alternatives}{Optional labels for the rows of \code{x}.}

\item{criteria}{Optional labels for the columns of \code{x}.}

\item{...}{Passed to the underlying method, for example \code{v} for VIKOR or
\code{lambda} for WASPAS.}
}
\value{
An object of class \code{sframe_sensitivity}, a list with \verb{$table} (one
row per criterion and direction, carrying \code{criterion}, \code{direction},
\code{rho}, \code{rank_changed}, and \code{top_changed}), \verb{$base_ranks}, \verb{$method},
\verb{$delta}, and \verb{$stable}, a single logical that is \code{TRUE} when no
perturbation changed the ranking.
}
\description{
Perturbs each criterion weight up and down by \code{delta}, renormalises the
weight vector to sum to 1, reruns the same ranking method, and compares
the perturbed ranking against the base ranking. A ranking that survives
this unchanged is one a reviewer can be told is robust to the weights. A
ranking whose leader changes under a 5 percent nudge is not.
}
\examples{
x <- matrix(c(4.1, 3.0, 210, 3.6, 4.5, 180, 4.8, 2.5, 260),
            nrow = 3, byrow = TRUE)
sa <- sensitivity_analysis(
  x,
  weights        = c(0.4, 0.3, 0.3),
  criteria_types = c("benefit", "benefit", "cost"),
  method         = "topsis",
  alternatives   = c("Alpha", "Basilica", "Coral"),
  criteria       = c("service", "location", "price")
)
sa$stable
sa$table
}
\seealso{
\code{\link[=run_analysis_plan]{run_analysis_plan()}}, \code{\link[=sframe_decision_options]{sframe_decision_options()}}
}
