% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decision_data.R
\name{sframe_rated_matrix}
\alias{sframe_rated_matrix}
\title{Build a performance matrix from rated matrix items}
\usage{
sframe_rated_matrix(data, instrument, items, statistic = c("mean", "median"))
}
\arguments{
\item{data}{A data frame of responses.}

\item{instrument}{An \code{sframe} instrument declaring every id in \code{items}.}

\item{items}{Character vector of \code{"matrix"} item ids, one per criterion, in
the intended criterion order. Every item must declare the same
\code{matrix_items} (the alternatives) in the same order.}

\item{statistic}{\code{"mean"} or \code{"median"}.}
}
\value{
A list with \code{matrix} (alternatives x criteria, with dimnames), \code{n},
\code{sd}, \code{alternatives}, \code{criteria}, and \code{statistic}.
}
\description{
The third collection path: respondents rate every alternative on every
criterion with ordinary matrix items, one item per criterion with the
alternatives as its rows, and the decision matrix is the per-cell
aggregate. No new item type is needed. Per-cell counts and standard
deviations are kept so the report can show how firm each cell is.
}
\seealso{
\code{\link[=sframe_collected_weights]{sframe_collected_weights()}}
}
