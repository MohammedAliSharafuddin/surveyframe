% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_ci.R
\name{bootstrap_ci}
\alias{bootstrap_ci}
\title{Percentile bootstrap confidence interval for a statistic}
\usage{
bootstrap_ci(x, FUN = stats::median, R = 2000, conf.level = 0.95, seed = NULL)
}
\arguments{
\item{x}{A numeric vector.}

\item{FUN}{A function of one vector returning a single number. Defaults
to \code{\link[stats:median]{stats::median()}}.}

\item{R}{Integer. Number of bootstrap resamples. Defaults to 2000.}

\item{conf.level}{Confidence level. Defaults to 0.95.}

\item{seed}{Integer or NULL. When supplied, sets the random seed so the
interval is reproducible.}
}
\value{
A named numeric vector: \code{estimate}, \code{lower}, \code{upper}. The bounds
are \code{NA} when \code{x} has fewer than 3 finite values.
}
\description{
Resamples \code{x} with replacement \code{R} times, applies \code{FUN} to each resample,
and returns the percentile interval of the resampled statistics together
with the observed value.
}
\examples{
bootstrap_ci(mtcars$mpg, seed = 42)
bootstrap_ci(mtcars$mpg, FUN = mean, conf.level = 0.90, seed = 42)
}
\seealso{
\code{\link[=cohens_d_ci]{cohens_d_ci()}}, \code{\link[=cramers_v_ci]{cramers_v_ci()}}, \code{\link[=eta_sq_ci]{eta_sq_ci()}}
}
