% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decision_data.R
\name{sframe_collected_weights}
\alias{sframe_collected_weights}
\title{Criterion weights collected from respondents}
\usage{
sframe_collected_weights(data, instrument, item_id, cr_filter = FALSE)
}
\arguments{
\item{data}{A data frame of responses.}

\item{instrument}{An \code{sframe} instrument declaring \code{item_id}.}

\item{item_id}{Character. A \code{"criteria_weight"} or \code{"pairwise_comparison"}
item id.}

\item{cr_filter}{Logical. Passed to \code{\link[=sframe_aggregate_judgements]{sframe_aggregate_judgements()}} for a
pairwise item. Ignored otherwise.}
}
\value{
A list with \code{weights} (a named numeric vector summing to 1),
\code{criteria}, \code{source}, \code{item_id}, \code{n_respondents}, \code{n_dropped}, and
\code{consistency} (pairwise items only).
}
\description{
Resolves a weight vector from either kind of collected weight item, so that
a ranking runner never has to know which one the researcher used. A
\code{"criteria_weight"} item is renormalised to sum 1 per respondent before the
arithmetic mean is taken, so a respondent who allocated 90 points in total
carries the same influence as one who allocated exactly 100. A
\code{"pairwise_comparison"} item is assembled, aggregated geometrically, and
reduced to its principal eigenvector.
}
\examples{
study <- sf_instrument(
  title = "Demo", components = list(
    sf_item("pts", "Divide 100 points", type = "criteria_weight",
            comparison_items = c("price", "speed"))
  )
)
sframe_collected_weights(
  data.frame(pts__price = c(60, 40), pts__speed = c(40, 60)), study, "pts"
)$weights
}
\seealso{
\code{\link[=sframe_assemble_pairwise]{sframe_assemble_pairwise()}}
}
