% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decision_data.R
\name{sframe_decision_options}
\alias{sframe_decision_options}
\title{Normalise the decision options of an analysis-plan block}
\usage{
sframe_decision_options(options)
}
\arguments{
\item{options}{The \code{options} list of a decision analysis block.}
}
\value{
The same list with \code{matrix} rebuilt as a numeric matrix carrying
dimnames, and \code{weights} and \code{criteria_types} coerced and checked.
}
\description{
A researcher-supplied performance matrix round-trips through JSON as a list
of numeric row vectors with its dimnames dropped, so it is stored as
\code{options$matrix} plus the \code{options$alternatives} and \code{options$criteria}
label vectors and rebuilt here. Every length agreement between the matrix,
its labels, the weights, and the criterion types is checked once, in one
place, with the exact mismatch named.
}
\examples{
sframe_decision_options(list(
  matrix = list(c(4, 210), c(3, 180)),
  alternatives = c("Alpha", "Basilica"),
  criteria = c("service", "price"),
  criteria_types = c("benefit", "cost")
))$matrix
}
\seealso{
\code{\link[=run_analysis_plan]{run_analysis_plan()}}
}
