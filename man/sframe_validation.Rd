% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation_result.R
\name{sframe_validation}
\alias{sframe_validation}
\alias{print.sframe_validation}
\alias{format.sframe_validation}
\alias{summary.sframe_validation}
\alias{as.data.frame.sframe_validation}
\title{Report on a validation result}
\usage{
\method{print}{sframe_validation}(x, ...)

\method{format}{sframe_validation}(x, ...)

\method{summary}{sframe_validation}(object, ...)

\method{as.data.frame}{sframe_validation}(x, row.names = NULL, optional = FALSE, ...)
}
\arguments{
\item{x, object}{An \code{sframe_validation} object.}

\item{...}{Ignored. Present for S3 consistency.}

\item{row.names}{Passed to \code{\link[base:as.data.frame]{base::as.data.frame()}}.}

\item{optional}{Passed to \code{\link[base:as.data.frame]{base::as.data.frame()}}.}
}
\value{
\code{print()} returns \code{x} invisibly. \code{format()} returns a single
character string. \code{summary()} returns the check table as a data frame.
\code{as.data.frame()} returns one row per problem.
}
\description{
\code{sframe_validation} is the diagnostic object returned by
\code{\link[=validate_sframe]{validate_sframe()}} and \code{\link[=validate_model]{validate_model()}}. It records whether the object
passed, every problem found, and every check that ran, including the checks
that found nothing.
}
\details{
Use \code{\link[=sf_is_valid]{sf_is_valid()}} for the pass or fail flag, \code{\link[=sf_problems]{sf_problems()}} for the
messages, \code{as.data.frame()} for a problem-per-row table, \code{summary()} for
the full check roster, and \code{\link[=as_sframe]{as_sframe()}} to recover the validated
instrument.
}
\examples{
cs    <- sf_choices("ag5", 1:5,
           c("Strongly disagree", "Disagree", "Neutral",
             "Agree", "Strongly agree"))
item  <- sf_item("sat_1", "The service met my expectations.",
                 type = "likert", choice_set = "ag5", scale_id = "sat")
scale <- sf_scale("sat", "Satisfaction", items = "sat_1")
instr <- sf_instrument("Demo Survey", components = list(cs, item, scale))

v <- validate_sframe(instr, strict = FALSE)
v
sf_is_valid(v)
sf_problems(v)
as.data.frame(v)
summary(v)
}
\seealso{
\code{\link[=validate_sframe]{validate_sframe()}}, \code{\link[=validate_model]{validate_model()}}, \code{\link[=sf_problems]{sf_problems()}},
\code{\link[=sf_is_valid]{sf_is_valid()}}, \code{\link[=as_sframe]{as_sframe()}}
}
