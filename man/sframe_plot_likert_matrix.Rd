% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{sframe_plot_likert_matrix}
\alias{sframe_plot_likert_matrix}
\title{Grouped diverging chart for a Likert matrix question}
\usage{
sframe_plot_likert_matrix(item, data, choice_set, palette = c("web", "print"))
}
\arguments{
\item{item}{A \code{"matrix"} sframe item, with \code{matrix_items} (the row
labels) and a \code{choice_set} naming the shared response scale.}

\item{data}{The response data.frame, with one expanded
\verb{<item id>__<row label>} column per matrix row, as produced by
\code{\link[=read_responses]{read_responses()}}.}

\item{choice_set}{The item's choice set object (\code{values}, \code{labels}),
typically looked up from \code{instrument$choices} by \code{item$choice_set}.}

\item{palette}{One of \code{"web"} or \code{"print"}. See \code{\link[=sframe_brand]{sframe_brand()}}.}
}
\value{
A ggplot2 object, or \code{NULL} if no row has response data.
}
\description{
A matrix question asks several rows against one shared response scale
(a "grid" of Likert items). Plotting each row as its own separate
\code{\link[=sframe_draw_likert_diverging]{sframe_draw_likert_diverging()}} chart loses the grouping the question
was designed with, so this draws every row as one diverging bar inside a
single chart, sharing one x scale and one legend, the standard way a
Likert matrix is reported (compare a typical multi-item satisfaction
grid). Same diverging-stack convention as the single-item chart: the
middle category of an odd-length scale is neutral and split evenly
across the zero line, and colour saturation increases toward each pole.
}
\seealso{
\code{\link[=sframe_draw_likert_diverging]{sframe_draw_likert_diverging()}}
}
