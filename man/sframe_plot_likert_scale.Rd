% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{sframe_plot_likert_scale}
\alias{sframe_plot_likert_scale}
\title{Grouped diverging chart for a scale's Likert items}
\usage{
sframe_plot_likert_scale(
  items,
  data,
  choice_set,
  title,
  palette = c("web", "print")
)
}
\arguments{
\item{items}{A list of \code{"likert"} sframe items belonging to one scale, in
display order.}

\item{data}{The response data.frame, with one column per item id.}

\item{choice_set}{The shared choice set object (\code{values}, \code{labels}).}

\item{title}{Chart title, typically the scale's label.}

\item{palette}{One of \code{"web"} or \code{"print"}. See \code{\link[=sframe_brand]{sframe_brand()}}.}
}
\value{
A ggplot2 object, or \code{NULL} if no item has response data.
}
\description{
Several \emph{separate} Likert items that make up one \code{\link[=sf_scale]{sf_scale()}} (unlike a
\code{"matrix"} item's rows, which are one question) are, by default, each
reported as their own single-item diverging chart. That scatters a
related batch of items (a satisfaction scale's 2-3 items, say) across
several charts instead of showing them the way a Likert matrix or a
typical multi-item satisfaction grid is reported: one grouped chart, one
diverging bar per item, sharing an x scale and a legend. Applies only
when every item in the scale shares the same choice set; scales that mix
response scales fall back to one chart per item.
}
\seealso{
\code{\link[=sframe_plot_likert_matrix]{sframe_plot_likert_matrix()}}, \code{\link[=sf_scale]{sf_scale()}}
}
