% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/google_sheets.R
\name{read_sheet_responses}
\alias{read_sheet_responses}
\title{Read survey responses from a Google Sheet}
\usage{
read_sheet_responses(
  sheet_id,
  instrument,
  sheet_name = "Responses",
  respondent_id = "respondent_id",
  submitted_at = "submitted_at",
  meta_cols = NULL
)
}
\arguments{
\item{sheet_id}{Character. The Google Sheet ID or full URL.}

\item{instrument}{An \code{sframe} object.}

\item{sheet_name}{Character. The name of the sheet tab holding responses.
Defaults to \code{"Responses"}.}

\item{respondent_id}{Character or NULL. Column holding respondent IDs.
Defaults to \code{"respondent_id"}.}

\item{submitted_at}{Character or NULL. Column holding submission
timestamps. Defaults to \code{"submitted_at"}.}

\item{meta_cols}{Character vector or NULL. Additional sheet columns to
accept as metadata without a warning, for example bridge fields a host
application appends to each submission. \code{"started_at"} is always
included.}
}
\value{
A \code{data.frame} validated against the instrument, ready for
\code{\link[=quality_report]{quality_report()}}, \code{\link[=score_scales]{score_scales()}}, and \code{\link[=reliability_report]{reliability_report()}}.
}
\description{
Reads response data collected by the surveyframe Google Apps Script
endpoint and returns a validated data frame ready for the surveyframe
analysis pipeline.
}
\examples{
\dontrun{
responses <- read_sheet_responses(
  sheet_id   = "your-sheet-id",
  instrument = instr
)
qr <- quality_report(responses, instr, respondent_id = "respondent_id")
}
}
\seealso{
\code{\link[=export_google_sheet]{export_google_sheet()}}, \code{\link[=read_responses]{read_responses()}}, \code{\link[=quality_report]{quality_report()}}
}
